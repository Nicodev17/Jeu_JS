/* class Cell{
    constructor(numeroCase, id, positionX, positionY) {
        this.listeCases = []; // Contient un tableau avec la liste des cases
        this.numeroCase = numeroCase;
        this.id = id;
        this.positionX = positionX;
        this.positionY = positionY;
    }

    infoCell(){
        console.log("Coordoonées de la case " + this.listeCases.numeroCase + " = X : " + this.listeCases.positionX + " / Y : " + this.listeCases.positionY);
    }
}
 */